# Отчет по Лабораторной работе №4

## Подготовил:
Рамазанов Эмиль Аликович

Студент группы М3100


## 1. Цели и задачи тестирования

### Цели:
- Обеспечить корректность работы функций вычисления площади и периметра для всех геометрических фигур: круг, прямоугольник, квадрат, треугольник
- Проверить правильность обработки граничных случаев (нулевые значения, дробные числа)
- Реализовать валидацию входных данных для защиты от некорректных значений (отрицательные числа)
- Автоматизировать процесс проверки функциональности библиотеки

### Задачи:
- Создать набор unit-тестов для всех модулей библиотеки (circle, rectangle, square, triangle)
- Покрыть тестами основные сценарии использования функций
- Протестировать граничные случаи и особые ситуации
- Реализовать валидацию входных данных с проверкой на отрицательные значения
- Добавить тесты для проверки обработки некорректных входных данных
- Обеспечить возможность автоматического запуска всех тестов

## 2. Описание тестируемого продукта

Библиотека `geometric_lib` представляет собой набор модулей для вычисления площади и периметра различных геометрических фигур:

- **circle.py** - вычисление площади и периметра круга
  - Функция `area(r)`: вычисляет площадь круга по формуле S = πR²
    - Валидация: проверяет, что радиус r ≥ 0, иначе выбрасывает ValueError
  - Функция `perimeter(r)`: вычисляет периметр круга по формуле P = 2πR
    - Валидация: проверяет, что радиус r ≥ 0, иначе выбрасывает ValueError

- **rectangle.py** - вычисление площади и периметра прямоугольника
  - Функция `area(a, b)`: вычисляет площадь прямоугольника по формуле S = ab
    - Валидация: проверяет, что длина a ≥ 0 и ширина b ≥ 0, иначе выбрасывает ValueError
  - Функция `perimeter(a, b)`: вычисляет периметр прямоугольника по формуле P = 2(a + b)
    - Валидация: проверяет, что длина a ≥ 0 и ширина b ≥ 0, иначе выбрасывает ValueError

- **square.py** - вычисление площади и периметра квадрата
  - Функция `area(a)`: вычисляет площадь квадрата по формуле S = a²
    - Валидация: проверяет, что сторона a ≥ 0, иначе выбрасывает ValueError
  - Функция `perimeter(a)`: вычисляет периметр квадрата по формуле P = 4a
    - Валидация: проверяет, что сторона a ≥ 0, иначе выбрасывает ValueError

- **triangle.py** - вычисление площади и периметра треугольника
  - Функция `area(a, h)`: вычисляет площадь треугольника по формуле S = ah/2
    - Валидация: проверяет, что основание a ≥ 0 и высота h ≥ 0, иначе выбрасывает ValueError
  - Функция `perimeter(a, b, c)`: вычисляет периметр треугольника по формуле P = a + b + c
    - Валидация: проверяет, что все стороны a, b, c ≥ 0, иначе выбрасывает ValueError

## 3. Область тестирования

### Модули, подлежащие тестированию:
1. **test_circle.py** - тестирование функций работы с кругом
2. **test_rectangle.py** - тестирование функций работы с прямоугольником
3. **test_square.py** - тестирование функций работы с квадратом
4. **test_triangle.py** - тестирование функций работы с треугольником

### Функциональность, подлежащая тестированию:
- Корректность вычисления площади для всех фигур
- Корректность вычисления периметра для всех фигур
- Обработка нормальных входных данных (положительные целые числа)
- Обработка граничных случаев (нулевые значения)
- Обработка дробных чисел
- Обработка единичных значений
- Валидация входных данных (обработка отрицательных чисел)
- Корректность выброса исключений при некорректных входных данных

## 4. Стратегия тестирования

### Типы тестирования:
- **Функциональное тестирование**: проверка соответствия функций их математическим формулам
- **Тестирование граничных значений**: проверка работы с нулевыми значениями и единичными значениями
- **Тестирование типов данных**: проверка работы с целыми и дробными числами
- **Тестирование обработки ошибок**: проверка корректной валидации входных данных и выброса исключений

### Методы тестирования:
- **Unit-тестирование**: изолированное тестирование отдельных функций
- **Автоматизированное тестирование**: использование фреймворка unittest для автоматического запуска тестов

### Техники тестирования:
- **Эквивалентное разбиение**: тестирование нормальных значений, граничных значений и особых случаев
- **Анализ граничных значений**: проверка работы с нулевыми и минимальными значениями
- **Таблицы решений**: проверка различных комбинаций входных параметров

## 5. Критерии приемки

Тестирование считается успешным, если:

1. Все unit-тесты проходят без ошибок
2. Покрытие тестами включает:
   - Нормальные случаи использования (положительные целые числа)
   - Граничные случаи (нулевые значения)
   - Дробные числа
   - Единичные значения
   - Некорректные входные данные (отрицательные числа)
3. Результаты вычислений соответствуют математическим формулам
4. Валидация входных данных работает корректно и выбрасывает соответствующие исключения
5. Все тесты могут быть запущены автоматически одной командой
6. Код тестов соответствует стандартам Python и использует unittest.TestCase

## 6. Ожидаемые результаты

### Ожидаемые метрики:
- **Количество тестов**: не менее 40 тестов (с учетом тестов валидации)
- **Покрытие модулей**: 100% (все 4 модуля)
- **Покрытие функций**: 100% (все функции area и perimeter)
- **Покрытие валидации**: 100% (все функции имеют проверку входных данных)
- **Процент успешных тестов**: 100%

### Ожидаемые артефакты:
- Набор тестовых файлов (test_*.py) для каждого модуля
- Отчет о выполнении тестов с указанием количества пройденных тестов

### Ожидаемый статус:
- Все тесты должны завершаться со статусом "OK"
- Время выполнения всех тестов должно быть минимальным (< 1 секунды)

## 7. Актуальные результаты тестирования

### Статистика выполнения тестов:

**Дата тестирования**: [текущая дата]

**Общая статистика**:
- Всего тестов: **52**
- Успешно пройдено: **52**
- Провалено: **0**
- Процент успешности: **100%**

### Детальные результаты по модулям:

#### test_circle.py
- Всего тестов: **10**
- Успешно: **10**
- Провалено: **0**
- Покрытие:
  - `area()`: 5 тестов (нормальные значения, нулевое значение, единичное значение, дробные числа, отрицательное значение)
  - `perimeter()`: 5 тестов (нормальные значения, нулевое значение, единичное значение, дробные числа, отрицательное значение)

#### test_rectangle.py
- Всего тестов: **16**
- Успешно: **16**
- Провалено: **0**
- Покрытие:
  - `area()`: 8 тестов (нормальные значения, нулевая ширина, нулевая высота, квадрат, дробные числа, отрицательная ширина, отрицательная высота, обе стороны отрицательные)
  - `perimeter()`: 8 тестов (нормальные значения, нулевая ширина, нулевая высота, квадрат, дробные числа, отрицательная ширина, отрицательная высота, обе стороны отрицательные)

#### test_square.py
- Всего тестов: **10**
- Успешно: **10**
- Провалено: **0**
- Покрытие:
  - `area()`: 5 тестов (нормальные значения, нулевое значение, единичное значение, дробные числа, отрицательное значение)
  - `perimeter()`: 5 тестов (нормальные значения, нулевое значение, единичное значение, дробные числа, отрицательное значение)

#### test_triangle.py
- Всего тестов: **16**
- Успешно: **16**
- Провалено: **0**
- Покрытие:
  - `area()`: 7 тестов (нормальные значения, нулевое основание, нулевая высота, дробные числа, отрицательное основание, отрицательная высота, оба параметра отрицательные)
  - `perimeter()`: 9 тестов (нормальные значения, равносторонний, нулевая сторона, дробные числа, равнобедренный, отрицательная первая сторона, отрицательная вторая сторона, отрицательная третья сторона, все стороны отрицательные)

### Пример выполнения тестов:

```
$ python3 -m unittest discover -v

test_area_float (test_circle.CircleTestCase.test_area_float) ... ok
test_area_negative (test_circle.CircleTestCase.test_area_negative) ... ok
test_area_normal (test_circle.CircleTestCase.test_area_normal) ... ok
...
----------------------------------------------------------------------
Ran 52 tests in 0.001s

OK
```

### Выводы:
- Все функции библиотеки работают корректно
- Граничные случаи обрабатываются правильно
- Дробные числа обрабатываются корректно
- Все математические формулы реализованы верно
- Валидация входных данных реализована во всех модулях
- Отрицательные значения корректно обрабатываются с выбросом ValueError
- Тесты для валидации покрывают все возможные комбинации отрицательных параметров
- Библиотека защищена от некорректных входных данных и готова к использованию
