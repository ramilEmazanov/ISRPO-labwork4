# 📐 Geometric Library

Простая и удобная Python-библиотека для вычисления площади и периметра основных геометрических фигур.

[![Python](https://img.shields.io/badge/Python-3.6+-blue.svg)](https://www.python.org/)
[![Tests](https://img.shields.io/badge/Tests-35%20passed-success.svg)]()

## 📋 Описание

`geometric_lib` — это легковесная библиотека, предоставляющая функции для вычисления площади и периметра следующих геометрических фигур:

- 🔵 **Круг** (Circle)
- 📦 **Прямоугольник** (Rectangle)
- ⬜ **Квадрат** (Square)
- 🔺 **Треугольник** (Triangle)

Библиотека включает полный набор unit-тестов, обеспечивающих корректность работы всех функций.

## 🚀 Быстрый старт

### Установка

Библиотека не требует дополнительных зависимостей (кроме стандартной библиотеки Python).


```bash
git clone https://github.com/yourusername/geometric_lib.git
cd geometric_lib
```

### Использование

```python
from circle import area, perimeter as circle_perimeter
from rectangle import area, perimeter as rectangle_perimeter
from square import area, perimeter as square_perimeter
from triangle import area, perimeter as triangle_perimeter

# Круг
circle_area = area(5)  # Площадь круга с радиусом 5
circle_perim = circle_perimeter(5)  # Периметр круга

# Прямоугольник
rect_area = area(4, 6)  # Площадь прямоугольника 4x6
rect_perim = rectangle_perimeter(4, 6)  # Периметр

# Квадрат
square_area = area(5)  # Площадь квадрата со стороной 5
square_perim = square_perimeter(5)  # Периметр

# Треугольник
triangle_area = area(10, 5)  # Площадь (основание=10, высота=5)
triangle_perim = triangle_perimeter(3, 4, 5)  # Периметр (стороны 3, 4, 5)
```

## 📚 API Документация

### Круг (`circle.py`)

```python
area(r)          # Вычисляет площадь круга
perimeter(r)     # Вычисляет периметр круга
```

**Формулы:**
- Площадь: S = πR²
- Периметр: P = 2πR

**Пример:**
```python
from circle import area, perimeter

area(5)        # 78.53981633974483
perimeter(5)   # 31.41592653589793
```

### Прямоугольник (`rectangle.py`)

```python
area(a, b)      # Вычисляет площадь прямоугольника
perimeter(a, b) # Вычисляет периметр прямоугольника
```

**Формулы:**
- Площадь: S = ab
- Периметр: P = 2(a + b)

**Пример:**
```python
from rectangle import area, perimeter

area(4, 6)      # 24
perimeter(4, 6) # 20
```

### Квадрат (`square.py`)

```python
area(a)         # Вычисляет площадь квадрата
perimeter(a)    # Вычисляет периметр квадрата
```

**Формулы:**
- Площадь: S = a²
- Периметр: P = 4a

**Пример:**
```python
from square import area, perimeter

area(5)         # 25
perimeter(5)    # 20
```

### Треугольник (`triangle.py`)

```python
area(a, h)           # Вычисляет площадь треугольника
perimeter(a, b, c)   # Вычисляет периметр треугольника
```

**Формулы:**
- Площадь: S = ah/2
- Периметр: P = a + b + c

**Пример:**
```python
from triangle import area, perimeter

area(10, 5)          # 25.0
perimeter(3, 4, 5)   # 12
```

## 🧪 Тестирование

Библиотека включает полный набор unit-тестов, написанных с использованием `unittest`.

### Запуск всех тестов

```bash
python3 -m unittest discover -v
```

### Запуск тестов для конкретного модуля

```bash
python3 -m unittest test_circle.py -v
python3 -m unittest test_rectangle.py -v
python3 -m unittest test_square.py -v
python3 -m unittest test_triangle.py -v
```

### Статистика тестов

- ✅ **Всего тестов:** 35
- ✅ **Успешно:** 35
- ❌ **Провалено:** 0
- 📊 **Покрытие:** 100%

Тесты покрывают:
- Нормальные случаи использования
- Граничные значения (нулевые значения)
- Дробные числа
- Единичные значения

## 📁 Структура проекта

```
geometric_lib/
├── circle.py           # Модуль для работы с кругом
├── rectangle.py        # Модуль для работы с прямоугольником
├── square.py           # Модуль для работы с квадратом
├── triangle.py         # Модуль для работы с треугольником
├── test_circle.py      # Тесты для circle.py
├── test_rectangle.py   # Тесты для rectangle.py
├── test_square.py      # Тесты для square.py
├── test_triangle.py    # Тесты для triangle.py
├── report.md           # Отчет по лабораторной работе
└── README.md           # Этот файл
```

## 📐 Математические формулы

### Площадь
- **Круг:** S = πR²
- **Прямоугольник:** S = ab
- **Квадрат:** S = a²
- **Треугольник:** S = ah/2

### Периметр
- **Круг:** P = 2πR
- **Прямоугольник:** P = 2a + 2b
- **Квадрат:** P = 4a
- **Треугольник:** P = a + b + c

## 👤 Автор

**Рамазанов Эмиль Аликович**

- Группа: М3100

## 📄 История изменений

- **v1.0.0** - Первоначальный релиз
  - Реализованы функции для круга, прямоугольника, квадрата и треугольника
  - Добавлены unit-тесты для всех модулей
  - Создана документация

---

